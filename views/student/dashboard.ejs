<!DOCTYPE html>
<html>
<head>
    <title>Student Voting Dashboard</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            margin: 0; 
            background: linear(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        
        .sidebar { 
            width: 280px; 
            background: rgba(255, 255, 255, 0.95); 
            color: #2c3e50; 
            height: 100vh; 
            padding: 30px 20px; 
            position: fixed;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            backdrop-filter: blur(10px);
        }
        
        .sidebar h2 {
            color: #27ae60;
            margin-bottom: 30px;
            text-align: center;
            font-size: 24px;
            border-bottom: 2px solid #27ae60;
            padding-bottom: 10px;
        }
        
        .main-content { 
            margin-left: 300px; 
            padding: 40px; 
            min-height: 100vh;
        }
        
        .header { 
            background: white; 
            padding: 30px; 
            border-radius: 15px; 
            margin-bottom: 30px; 
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            text-align: center;
        }
        
        .header h1 {
            color: #2c3e50;
            margin-bottom: 10px;
            font-size: 2.5em;
        }
        
        .header p {
            color: #7f8c8d;
            font-size: 1.1em;
        }
        
        .election-card { 
            background: white; 
            padding: 25px; 
            border-radius: 15px; 
            margin-bottom: 20px; 
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            border-left: 5px solid #27ae60;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .election-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        }
        
        .election-title { 
            color: #2c3e50; 
            margin-top: 0; 
            font-size: 1.5em;
            margin-bottom: 10px;
        }
        
        .election-meta { 
            color: #7f8c8d; 
            margin: 8px 0; 
            line-height: 1.5;
        }
        
        .status { 
            display: inline-block; 
            padding: 8px 15px; 
            border-radius: 20px; 
            font-size: 0.9em; 
            margin: 10px 0; 
            font-weight: bold;
        }
        
        .status-upcoming { 
            background: #fff3cd; 
            color: #856404; 
            border: 1px solid #ffeaa7;
        }
        
        .status-ongoing { 
            background: #d4edda; 
            color: #155724; 
            border: 1px solid #c3e6cb;
        }
        
        .status-completed { 
            background: #d1ecf1; 
            color: #0c5460; 
            border: 1px solid #bee5eb;
        }
        
        .btn { 
            display: inline-block; 
            padding: 12px 25px; 
            background: #27ae60; 
            color: white; 
            text-decoration: none; 
            border-radius: 8px; 
            margin-right: 10px; 
            margin-top: 10px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            font-size: 14px;
        }
        
        .btn:hover { 
            background: #219653; 
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        
        .btn-disabled { 
            background: #95a5a6; 
            cursor: not-allowed;
        }
        
        .btn-disabled:hover {
            transform: none;
            box-shadow: none;
        }
        
        .btn-vote { 
            background: #e74c3c; 
        }
        
        .btn-vote:hover {
            background: #c0392b;
        }
        
        .btn-results { 
            background: #3498db; 
        }
        
        .btn-results:hover {
            background: #2980b9;
        }
        
        .btn-register { 
            background: #f39c12; 
            color: white;
        }
        
        .btn-register:hover {
            background: #e67e22;
        }
        
        .sidebar a { 
            color: #2c3e50; 
            text-decoration: none; 
            display: block; 
            padding: 15px; 
            border-radius: 8px; 
            margin: 8px 0; 
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        .sidebar a:hover { 
            background: #27ae60; 
            color: white;
            transform: translateX(5px);
        }
        
        .sidebar a.active { 
            background: #27ae60; 
            color: white;
        }
        
        .stats { 
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px; 
            margin-bottom: 40px; 
        }
        
        .stat-card { 
            background: white; 
            padding: 25px; 
            border-radius: 15px; 
            text-align: center; 
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
        }
        
        .stat-number { 
            font-size: 3em; 
            font-weight: bold; 
            color: #27ae60; 
            margin: 15px 0; 
        }
        
        .welcome-banner {
            background: linear(135deg, #27ae60, #2ecc71);
            color: white;
            padding: 20px;
            border-radius: 15px;
            margin-bottom: 30px;
            text-align: center;
        }
        
        .action-buttons {
            display: flex;
            gap: 15px;
            margin: 20px 0;
            flex-wrap: wrap;
        }
        
        .no-elections {
            background: white;
            padding: 50px 30px;
            text-align: center;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .no-elections h3 {
            color: #7f8c8d;
            margin-bottom: 15px;
        }
        
        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }
            .main-content {
                margin-left: 0;
            }
            .stats {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2>🎓 Student Voting</h2>
        <a href="/student/dashboard" class="active">📊 My Dashboard</a>
        <a href="/student/elections">🗳️ All Elections</a>
        <a href="/logout">🚪 Logout</a>
        
        <div style="margin-top: 30px; padding: 15px; background: #f8f9fa; border-radius: 8px;">
            <p style="font-size: 0.9em; color: #666; margin-bottom: 10px;"><strong>Quick Guide:</strong></p>
            <p style="font-size: 0.8em; color: #888; line-height: 1.4;">
                • Click <strong>Vote Now</strong> to cast your vote<br>
                • <strong>Register</strong> to run as candidate<br>
                • View <strong>Results</strong> after voting ends
            </p>
        </div>
    </div>
    
    <div class="main-content">
        <div class="header">
            <h1>Student Voting Portal</h1>
            <p>Welcome back, <strong><%= user.full_name %></strong>! (ID: <%= user.student_id %>)</p>
        </div>

        <div class="welcome-banner">
            <h2>🎯 Ready to Make Your Voice Heard!</h2>
            <p>Participate in elections and shape your college community</p>
        </div>

        <div class="stats">
            <div class="stat-card">
                <h3>Active Elections</h3>
                <div class="stat-number">
                    <%= elections ? elections.filter(e => e.status === 'ongoing').length : 0 %>
                </div>
                <p>Ready for Voting</p>
            </div>
            <div class="stat-card">
                <h3>My Candidates</h3>
                <div class="stat-number">
                    <%= elections ? elections.filter(e => e.is_candidate > 0).length : 0 %>
                </div>
                <p>Elections I'm Running In</p>
            </div>
            <div class="stat-card">
                <h3>My Votes</h3>
                <div class="stat-number">
                    <%= elections ? elections.filter(e => e.has_voted > 0).length : 0 %>
                </div>
                <p>Votes Cast</p>
            </div>
        </div>

        <h2 style="color: white; margin-bottom: 20px; font-size: 1.8em;">📋 Available Elections</h2>
        
        <% if (elections && elections.length > 0) { %>
            <% let hasActiveElections = elections.some(e => e.status === 'ongoing'); %>
            
            <% if (!hasActiveElections) { %>
                <div class="no-elections">
                    <h3>⏳ No Active Elections Right Now</h3>
                    <p>Check back later for upcoming voting opportunities!</p>
                    <a href="/student/elections" class="btn btn-results" style="display: inline-block; margin-top: 15px;">
                        View All Elections
                    </a>
                </div>
            <% } %>
            
            <% elections.forEach(election => { %>
                <div class="election-card">
                    <h2 class="election-title">🏛️ <%= election.title %></h2>
                    <p class="election-meta"><%= election.description %></p>
                    <p class="election-meta">
                        <strong>🗓️ Voting Period:</strong> 
                        <%= new Date(election.start_date).toLocaleDateString() %> 
                        to 
                        <%= new Date(election.end_date).toLocaleDateString() %>
                    </p>
                    
                    <!-- Display election status -->
                    <% let statusClass = '';
                       let statusIcon = '';
                       if (election.status === 'upcoming') {
                           statusClass = 'status-upcoming';
                           statusIcon = '⏰';
                       } else if (election.status === 'ongoing') {
                           statusClass = 'status-ongoing';
                           statusIcon = '✅';
                       } else if (election.status === 'completed') {
                           statusClass = 'status-completed';
                           statusIcon = '🏁';
                       }
                    %>
                    <div class="status <%= statusClass %>">
                        <%= statusIcon %> <strong>Status:</strong> <%= election.status.toUpperCase() %>
                    </div>
                    
                    <div class="action-buttons">
                        <!-- Conditionally show buttons based on election status and user actions -->
                        <% if (election.status === 'upcoming') { %>
                            <div style="flex: 1;">
                                <p style="color: #666; margin-bottom: 10px;">📝 Election starts soon</p>
                                <% if (election.is_candidate > 0) { %>
                                    <p style="color: #27ae60; font-weight: bold;">🎯 You are registered as candidate</p>
                                <% } else { %>
                                    <a href="/student/register-candidate/<%= election.id %>" class="btn btn-register">
                                        ✍️ Register as Candidate
                                    </a>
                                <% } %>
                            </div>
                        <% } else if (election.status === 'ongoing') { %>
                            <div style="flex: 1;">
                                <% if (election.has_voted > 0) { %>
                                    <p style="color: #27ae60; font-weight: bold; font-size: 1.1em;">
                                        ✅ You have voted in this election
                                    </p>
                                    <a href="/student/results/<%= election.id %>" class="btn btn-results">
                                        📊 View Live Results
                                    </a>
                                <% } else if (election.is_candidate > 0) { %>
                                    <p style="color: #e74c3c; font-weight: bold;">🎯 You are running as candidate</p>
                                    <a href="/student/vote/<%= election.id %>" class="btn btn-vote">
                                        🗳️ View Voting Page
                                    </a>
                                <% } else { %>
                                    <a href="/student/vote/<%= election.id %>" class="btn btn-vote" style="font-size: 16px; padding: 15px 30px;">
                                        🗳️ VOTE NOW
                                    </a>
                                    <a href="/student/register-candidate/<%= election.id %>" class="btn btn-register">
                                        ✍️ Register as Candidate
                                    </a>
                                <% } %>
                            </div>
                        <% } else if (election.status === 'completed') { %>
                            <div style="flex: 1;">
                                <p style="color: #666; margin-bottom: 10px;">🏁 Election has ended</p>
                                <a href="/student/results/<%= election.id %>" class="btn btn-results">
                                    📊 View Final Results
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            <% }); %>
        <% } else { %>
            <div class="no-elections">
                <h3>📭 No Elections Available</h3>
                <p>There are no elections scheduled at the moment.</p>
                <p style="color: #888; margin-top: 10px;">Check with your administrator for upcoming elections.</p>
            </div>
        <% } %>
        
        <div style="text-align: center; margin-top: 40px; color: white;">
            <p>Need help? Contact your election administrator.</p>
        </div>
    </div>
</body>
</html>