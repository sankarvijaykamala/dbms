<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard</title>
    <style>
        body { font-family: Arial; margin: 0; background: #f5f5f5; }
        .sidebar { width: 250px; background: #2c3e50; color: white; height: 100vh; padding: 20px; position: fixed; }
        .main-content { margin-left: 270px; padding: 30px; }
        .header { background: white; padding: 20px; border-radius: 5px; margin-bottom: 30px; }
        .stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-bottom: 30px; }
        .stat-card { background: white; padding: 20px; border-radius: 5px; text-align: center; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        .stat-number { font-size: 2em; font-weight: bold; color: #4CAF50; margin: 10px 0; }
        .btn { background: #4CAF50; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px; display: inline-block; margin: 5px; }
        a { color: white; text-decoration: none; display: block; padding: 10px; border-radius: 5px; margin: 5px 0; }
        a:hover { background: #34495e; }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2>Admin Panel</h2>
        <a href="/admin/dashboard" style="background: #34495e;">📊 Dashboard</a>
        <a href="/admin/create-election">➕ Create Election</a>
        <a href="/admin/elections">🗳️ View Elections</a>
        <a href="/logout">🚪 Logout</a>
    </div>
    
    <div class="main-content">
        <div class="header">
            <h1>Admin Dashboard</h1>
            <p>Welcome, <%= user.full_name %>!</p>
        </div>
        
        <div class="stats">
            <div class="stat-card">
                <h3>Total Elections</h3>
                <div class="stat-number"><%= stats.total_elections %></div>
            </div>
            <div class="stat-card">
                <h3>Total Candidates</h3>
                <div class="stat-number"><%= stats.total_candidates %></div>
            </div>
            <div class="stat-card">
                <h3>Total Students</h3>
                <div class="stat-number"><%= stats.total_students %></div>
            </div>
        </div>
        
        <div style="background: white; padding: 20px; border-radius: 5px;">
            <h2>Quick Actions</h2>
            <a href="/admin/create-election" class="btn">Create New Election</a>
            <a href="/admin/elections" class="btn" style="background: #6c757d;">Manage Elections</a>
        </div>
    </div>
</body>
</html>