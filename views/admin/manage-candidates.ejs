<!DOCTYPE html>
<html>
<head>
    <title>Manage Candidates - Admin</title>
    <style>
        body { font-family: Arial; margin: 0; background: #f5f5f5; }
        .sidebar { width: 250px; background: #2c3e50; color: white; height: 100vh; padding: 20px; position: fixed; }
        .main-content { margin-left: 270px; padding: 30px; }
        .header { background: white; padding: 20px; border-radius: 5px; margin-bottom: 30px; }
        .candidate-card { background: white; padding: 20px; border-radius: 5px; margin-bottom: 15px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        .btn { display: inline-block; padding: 8px 15px; margin: 5px; text-decoration: none; border-radius: 5px; color: white; font-size: 14px; border: none; cursor: pointer; }
        .btn-approve { background: #28a745; }
        .btn-reject { background: #dc3545; }
        .btn-delete { background: #6c757d; }
        .status-pending { color: #ffc107; font-weight: bold; }
        .status-approved { color: #28a745; font-weight: bold; }
        .status-rejected { color: #dc3545; font-weight: bold; }
        a { color: white; text-decoration: none; display: block; padding: 10px; border-radius: 5px; margin: 5px 0; }
        a:hover { background: #34495e; }
        .candidate-info { margin-bottom: 10px; }
        .manifesto { font-style: italic; color: #666; margin: 10px 0; }
        .no-candidates { background: white; padding: 30px; text-align: center; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2>Admin Panel</h2>
        <a href="/admin/dashboard">📊 Dashboard</a>
        <a href="/admin/create-election">➕ Create Election</a>
        <a href="/admin/elections">🗳️ View Elections</a>
        <a href="/admin/manage-candidates/<%= election.id %>" style="background: #34495e;">👥 Manage Candidates</a>
        <a href="/logout">🚪 Logout</a>
    </div>
    
    <div class="main-content">
        <div class="header">
            <h1>Manage Candidates</h1>
            <p><strong>Election:</strong> <%= election.title %></p>
            <p>Welcome, <%= user.full_name %>!</p>
        </div>
        
        <% if (candidates.length === 0) { %>
            <div class="no-candidates">
                <h3>No candidates registered yet</h3>
                <p>Students need to register as candidates for this election.</p>
                <a href="/admin/elections" style="display: inline-block; background: #6c757d;">← Back to Elections</a>
            </div>
        <% } else { %>
            <div style="margin-bottom: 20px;">
                <a href="/admin/elections" style="display: inline-block; background: #6c757d;">← Back to Elections</a>
            </div>
            
            <% candidates.forEach(candidate => { %>
            <div class="candidate-card">
                <div class="candidate-info">
                    <h3><%= candidate.full_name %></h3>
                    <p><strong>Student ID:</strong> <%= candidate.student_id %></p>
                    <p><strong>Position:</strong> <%= candidate.position %></p>
                    <p><strong>Status:</strong> 
                        <span class="status-<%= candidate.status %>">
                            <%= candidate.status.charAt(0).toUpperCase() + candidate.status.slice(1) %>
                        </span>
                    </p>
                    <% if (candidate.manifesto) { %>
                        <div class="manifesto">"<%= candidate.manifesto %>"</div>
                    <% } %>
                    <p><strong>Registered:</strong> <%= new Date(candidate.registration_date).toLocaleString() %></p>
                </div>
                
                <div>
                    <% if (candidate.status === 'pending') { %>
                        <form method="POST" action="/admin/approve-candidate/<%= candidate.id %>" style="display: inline;">
                            <button type="submit" class="btn btn-approve">✅ Approve</button>
                        </form>
                        <form method="POST" action="/admin/reject-candidate/<%= candidate.id %>" style="display: inline;">
                            <button type="submit" class="btn btn-reject">❌ Reject</button>
                        </form>
                    <% } %>
                    
                    <form method="POST" action="/admin/delete-candidate/<%= candidate.id %>" style="display: inline;">
                        <button type="submit" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this candidate?')">🗑️ Delete</button>
                    </form>
                </div>
            </div>
            <% }); %>
        <% } %>
    </div>
</body>
</html>